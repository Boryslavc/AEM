# Docker Compose: copy to .env and adjust as needed.
# See README "Environment variables" for which service uses each variable.

# Postgres (used by content-service via DATABASE_URL in compose)
POSTGRES_DB=aem_content
POSTGRES_USER=aem
POSTGRES_PASSWORD=aem_password_db

# Optional: protect edge-cache /internal/cache (set same value in both places)
# EDGE_CACHE_INTERNAL_SECRET=
